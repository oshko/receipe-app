{
  "name": "recipe-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Recipe app — React client, Express API, PostgreSQL storage",
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "concurrently -n api,web -c yellow,cyan \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "dev:api": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace client",
    "build": "npm run build --workspace client",
    "seed": "npm run seed --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
